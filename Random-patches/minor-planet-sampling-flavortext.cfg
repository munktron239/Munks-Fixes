// gives some description hints if you have MinorPlanetSampling installed
// might move this patch to KSP2 names eventually
@PART[SurfaceScanner]:NEEDS[MinorPlanetSampling]	{
@description ^= :$:\n\nThis module can also collect and store a sample from a captured asteroid or comet, allowing for robotic retrieval of otherworldly data! Warranty void if used to collect samples from suspicious snacks.			}
