// Reliant: Kerolox launcher engine with exhaust dump (Restock only)
@PART[liquidEngine_v2]:NEEDS[StockWaterfallEffects,Restock]:LAST[Waterfall]
{
	MODULE
	{
		name = ModuleWaterfallFX
		// This is a custom name
		moduleID = reliantTurboFX
		// This links the effects to a given ModuleEngines
		engineID = basicEngine

		// List out all controllers we want available
		// This controller scales with atmosphere depth
		CONTROLLER
		{
			name = atmosphereDepth
			linkedTo = atmosphere_density
		}
		// This controller scales with effective throttle
		CONTROLLER
		{
			name = throttle
			linkedTo = throttle
		}
		// this controller generates a random value in the range specified
		CONTROLLER
		{
			name = random
			linkedTo = random
			range = -1,1
		}
		// -----------------------------------------------------
		// Past here should be generated with the ingame editor!
		// -----------------------------------------------------
		TEMPLATE
		{
			// This is the name of the template to use
			name = exhaust
			templateName = stock-kerolox-generator
			// This field allows you to override the parentTransform name in the EFFECTS contained in the template
			overrideParentTransform = thrustTransform
			position = -0.032,0.00015,0.0193
			rotation = 0, -15, 0
			scale = 0.33, 0.33, 0.165
		}
	}
}